on:
  push:
    branches:
      - master

jobs:
  gprActionJob:
    name: GPR Action Job
    runs-on: ubuntu-latest
    steps:
        - uses: actions/setup-node@v1
          with:
            node-version: 10.x